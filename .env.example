# Supabase (Dashboard → Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL="https://numeadffsfidwrcypaej.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-or-publishable-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-or-secret-key"

# App URLs
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_SITE_NAME="Kameraz.com"
AUTH_URL="http://localhost:3000"

# Şəkillər Supabase Storage-da saxlanır (Cloudinary lazım deyil)
# SQL: supabase/03-storage.sql (bir dəfə) — və ya ilk upload avtomatik bucket yaradır

# Köhnə Prisma/NextAuth env-ləri artıq lazım deyil:
# DATABASE_URL, DIRECT_URL, AUTH_SECRET, NEXTAUTH_URL, CLOUDINARY_*

# Supabase SQL Editor:
# 1) supabase/full-setup.sql
# 2) supabase/03-storage.sql
# Authentication → Users → admin yarat
